# birthday-card

This project contains a dynamic birthday card for Eunice's 29th birthday built with React, Bootstrap and animate.css.

Open `index.html` in a modern browser to view the card. It displays greetings from friends with animated effects and a festive image.

To customize the friend messages, edit the `greetings` array inside the `BirthdayCard` component in `index.html`.
